<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javacode.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.javacode.entity.User">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="CHAR" property="name" />
    <result column="phone" jdbcType="CHAR" property="phone" />
    <result column="type" jdbcType="CHAR" property="type" />
    <result column="password" jdbcType="CHAR" property="password" />

  </resultMap>
  <sql id="Base_Column_List">
    id,name,phone,type,password
  </sql>

  <select id="findByName" resultType="com.javacode.entity.User">
    select * from user where 1=1
    <if test="name !=null and name !='all' and name !=''">
      and `name` = #{name}
    </if>
  </select>

  <select id="checkRepeat" resultType="java.lang.Integer">
    select count(*) from user where ${column} = #{value}
  </select>

</mapper>