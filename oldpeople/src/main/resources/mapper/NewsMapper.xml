<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javacode.mapper.NewsMapper">
	<select id="findByTitle" resultType="com.javacode.entity.News">
		select * from news where 1=1
		<if test="title !=null and title !='all' and title !=''">
			and `title` like concat('%',#{title},'%')
		</if>
		order by `time` desc
	</select>
	<select id="findByDiseaseName" resultType="com.javacode.entity.News">
		SELECT DISTINCT n.*
		FROM news n
		WHERE n.title LIKE CONCAT('%', #{diseaseName}, '%')
	</select>
	<!-- NewsMapper.xml -->

	<!-- 其他 SQL 语句... -->

	<!-- 获取所有新闻类型 -->
	<select id="getAllNewsTypes" resultType="string">
		SELECT DISTINCT type FROM news;
	</select>

	<!-- 更新新闻记录 -->
	<update id="update" parameterType="com.javacode.entity.News">
		UPDATE news
		SET title = #{title},
		content = #{content},
		table = #{table},
		time = #{time},
		WHERE id = #{id}
	</update>

	<select id="findByTitleContaining" resultType="com.javacode.entity.News">
		SELECT * FROM news WHERE title LIKE CONCAT('%', #{title}, '%')
	</select>

</mapper>