<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javacode.mapper.VideoMapper">
  <resultMap id="BaseResultMap" type="com.javacode.entity.Video">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="name" jdbcType="CHAR" property="name" />
    <result column="phone" jdbcType="CHAR" property="address" />

  </resultMap>
  <sql id="Base_Column_List">
    id,name,address
  </sql>

  <select id="findByName" resultType="com.javacode.entity.Video">
    select * from video where 1=1
    <if test="name !=null and name !='all' and name !=''">
      and `name` = #{name}
    </if>
  </select>

  <select id="checkRepeat" resultType="java.lang.Integer">
    select count(*) from video where ${column} = #{value}
  </select>

</mapper>